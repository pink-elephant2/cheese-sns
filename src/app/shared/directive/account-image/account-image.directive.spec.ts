import { AccountImageDirective } from './account-image.directive';

describe('AccountImageDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountImageDirective();
    expect(directive).toBeTruthy();
  });
});
